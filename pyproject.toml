[project]
name = "ridehail"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.12.11",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
